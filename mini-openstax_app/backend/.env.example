# Example environment variables for Mini OpenStax Backend
# DO NOT commit real secrets! Copy this to .env and fill in values locally.

# JWT Secret (must be 32+ random characters)
JWT_SECRET=your_strong_random_32+_char_secret_here

# PostgreSQL Database URL
# Local example:
DATABASE_URL=postgres://openstax_user:openstax_pass@localhost:5432/openstax_db

# Node environment
NODE_ENV=development

# Optional: Frontend URL for CORS (used in server.js)
FRONTEND_URL=http://localhost:8080
